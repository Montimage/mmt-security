<beginning>
<!-- Detection Rule 207: Detect DICOM PDU length exceeding 16MB (16777216) -->
<embedded_functions><![CDATA[
#include <mmt_core.h>
static int em_is_dicom(const void *data) {
    proto_hierarchy_t *p = (proto_hierarchy_t *) data;
    int i;
    for (i = 0; i < p->len; i++)
        if (p->proto_path[i] == 701)
            return 1;
    return 0;
}
]]></embedded_functions>
<property property_id="207" type_property="ATTACK"
     description="Detect DICOM PDU length exceeding 16MB">
    <event description="DICOM packet with PDU length exceeding 16MB"
        boolean_expression="((ip.src != 0) &amp;&amp; (#em_is_dicom(meta.proto_hierarchy) == 1) &amp;&amp; (dicom.pdu_len &gt; 16777216))"/>
</property>
</beginning>
