<beginning>
<embedded_functions><![CDATA[
static void em_modif_then_forward(
	   const rule_info_t *rule, int verdict, uint64_t timestamp, 
	   uint64_t counter, const mmt_array_t * const trace ){	
  	const char* trace_str = mmt_convert_execution_trace_to_json_string( trace, rule );
   	printf( " Rule 97:Recognized suspect packet \n");

     	
	}


]]></embedded_functions>

<property value="THEN" delay_units="ms" delay_min="0" delay_max="100" property_id="97" type_property="ATTACK" 
    description="5G Http2 DoS attack Recognition" if_satisfied="em_modif_then_forward">
    <event value="COMPUTE" event_id="1" 
        description="HTTP2 Compression attack"
           boolean_expression="( (http2.header_length>350) )"/>

 <event value="COMPUTE" event_id="2" 
           description="Nothing"
           boolean_expression="( http2.header_method != 0)"/>  
</property>
</beginning>
