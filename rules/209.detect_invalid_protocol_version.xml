<beginning>
<!-- Detection Rule 209: Detect invalid DICOM protocol version (not 1) in A-ASSOCIATE-RQ -->
<embedded_functions><![CDATA[
#include <mmt_core.h>
static int em_is_dicom(const void *data) {
    proto_hierarchy_t *p = (proto_hierarchy_t *) data;
    int i;
    for (i = 0; i < p->len; i++)
        if (p->proto_path[i] == 701)
            return 1;
    return 0;
}
]]></embedded_functions>
<property property_id="209" type_property="ATTACK"
     description="Detect invalid DICOM protocol version">
    <event description="DICOM A-ASSOCIATE-RQ or A-ASSOCIATE-AC with protocol version != 1"
        boolean_expression="((ip.src != 0) &amp;&amp; (#em_is_dicom(meta.proto_hierarchy) == 1) &amp;&amp; (dicom.pdu_type &gt; 0) &amp;&amp; (dicom.pdu_type &lt; 3) &amp;&amp; (dicom.proto_version != 1))"/>
</property>
</beginning>
